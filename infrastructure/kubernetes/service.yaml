apiVersion: v1
kind: Service
metadata:
  name: devops-chatbot-service
  namespace: default
  labels:
    app: devops-chatbot
spec:
  selector:
    app: devops-chatbot
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8000
  type: LoadBalancer
